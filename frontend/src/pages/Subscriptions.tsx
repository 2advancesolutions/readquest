/**
 * Subscriptions — Dedicated full-page subscription plans view.
 * Wraps the reusable SubscriptionPlans component in full-page mode.
 */
import SubscriptionPlans from '../components/SubscriptionPlans'

export default function Subscriptions() {
  return <SubscriptionPlans fullPage />
}
